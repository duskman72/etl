import { Dialog } from "./Dialog";

export const CredentialsDialog = () => {
    const id = "credentialsDialog";
    return <Dialog id={id} title={"Credentials"} onOk={() => {}}>
        TBD IMPLEMT CRED CHILDREN
    </Dialog>
}